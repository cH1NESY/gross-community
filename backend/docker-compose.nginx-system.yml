# Дополнительный файл для настройки PHP-FPM с пробросом порта
# Используйте этот файл как пример изменений для docker-compose.yml

# В секции php-fpm добавьте:
#   ports:
#     - "127.0.0.1:9000:9000"

# И уберите или закомментируйте сервис web (nginx-gross), 
# так как теперь используется системный Nginx

